{
  "name": "atmosphere",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/atmosphere/src",
  "projectType": "library",
  "tags": ["type:lib"],
  "// targets": "to see all targets run: nx show project atmosphere --web",
  "targets": {
    "build": {
      "dependsOn": ["typecheck"]
    }
  }
}
